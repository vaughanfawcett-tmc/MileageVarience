# Virtual env / caches
.venv/
__pycache__/
*.pyc
.pytest_cache/
.DS_Store

# DATA — never commit. Contains driver PII (names, emails, phones, postcodes).
*.xlsx
*.xls
*.csv

# Local report history store (the production copy lives on the Render disk).
.data/
*.db
*.pkl.gz

# Internal project brief
TMC Project Brief*.md

# Local secrets (Render secrets are set in the dashboard, not committed)
.streamlit/secrets.toml
.env
